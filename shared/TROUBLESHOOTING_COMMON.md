# Общие проблемы и решения

Проблемы, которые могут возникнуть в обоих практикумах (`genome_annotation` и `variant_calling`).

Для специфичных проблем конкретного модуля см. `docs/troubleshooting.md` в папке модуля.

---

## Установка и окружения

### ❌ "command not found: micromamba"

**Где видно:** При попытке `micromamba activate`

**Причины:**
- Micromamba не установлен
- Не добавлен в PATH

**Решение:**

```bash
# Проверьте, установлен ли:
which micromamba

# Если пусто, установите:
curl -L https://micro.mamba.pm/install.sh | bash

# Или используйте conda вместо micromamba:
conda activate ngs-annotation
```

---

### ❌ "No module named 'XYZ' (например, 'seqkit', 'bcftools')"

**Где видно:** При запуске `00_check_system.sh` или скриптов

**Причины:**
- Окружение не активировано
- Окружение не создано корректно

**Решение:**

```bash
# Убедитесь, что окружение АКТИВИРОВАНО:
micromamba activate ngs-annotation    # для genome_annotation
# или
micromamba activate ngs-variants      # для variant_calling

# Проверьте, какое окружение активно:
micromamba env list

# Если окружение отсутствует, создайте заново:
bash env/micromamba_create.sh
```

---

### ❌ "CondaError: Unsatisfiable dependencies"

**Где видно:** При создании окружения

**Причины:**
- Версии пакетов конфликтуют
- Неподдерживаемая версия Python
- Сетевые проблемы при скачивании

**Решение:**

```bash
# Очистите и попробуйте заново:
micromamba env remove -n ngs-annotation
micromamba env remove -n ngs-variants

# Обновите micromamba/conda:
micromamba update -n base -c conda-forge micromamba

# Пересоздайте окружение:
bash genome_annotation/env/micromamba_create.sh
bash variant_calling/env/micromamba_create.sh
```

---

## Скрипты и выполнение

### ❌ "Permission denied" при запуске скриптов

**Где видно:** `bash scripts/00_check_system.sh`

**Причины:**
- Скрипты не имеют прав на исполнение

**Решение:**

```bash
# Добавьте права на исполнение:
chmod +x genome_annotation/scripts/*.sh
chmod +x genome_annotation/env/*.sh
chmod +x variant_calling/scripts/*.sh
chmod +x variant_calling/env/*.sh
chmod +x variant_calling/workflow/*.sh
chmod +x genome_annotation/workflow/*.sh
```

---

### ❌ "bash: ./scripts/XX_*.sh: line 2: syntax error near unexpected token..."

**Где видно:** При запуске скрипта

**Причины:**
- Файл имеет неправильные окончания строк (Windows CRLF вместо Unix LF)
- Повреждённый файл

**Решение:**

```bash
# Конвертируйте окончания строк (все скрипты):
find . -name "*.sh" -type f -exec dos2unix {} \;

# Если dos2unix не установлен:
sed -i 's/\r$//' genome_annotation/scripts/*.sh
sed -i 's/\r$//' variant_calling/scripts/*.sh
```

---

### ❌ "set: No such option" или "shopt: not found"

**Где видно:** При запуске скриптов

**Причины:**
- Скрипт запущен с `sh` вместо `bash`
- Неправильный шебанг

**Решение:**

```bash
# Запустите правильно:
bash scripts/00_check_system.sh     # ✓ Правильно

# Не делайте так:
sh scripts/00_check_system.sh       # ✗ Неправильно
./scripts/00_check_system.sh        # ✗ Без bash
```

---

## Данные и файлы

### ❌ "File not found: data/input/assembly.fasta" или аналог

**Где видно:** При запуске рабочих скриптов (10_run_prokka.sh и т.д.)

**Причины:**
- Скрипт `01_get_data.sh` не был запущен
- Данные не скачались полностью
- Неправильный путь в config.sh

**Решение:**

```bash
# Убедитесь, что запустили подготовку:
bash scripts/01_get_data.sh

# Проверьте, что файлы существуют:
ls -lh data/input/
ls -lh data/truth/          # для variant_calling

# Если файлов нет, запустите ещё раз (может понадобиться Internet):
bash scripts/01_get_data.sh
```

---

### ❌ "No such file or directory: workflow/config.sh"

**Где видно:** При запуске любого рабочего скрипта

**Причины:**
- Забыли скопировать config.sh.example в config.sh

**Решение:**

```bash
cd genome_annotation
cp workflow/config.sh.example workflow/config.sh

# Или для variant_calling:
cd ../variant_calling
cp workflow/config.sh.example workflow/config.sh
```

---

### ❌ "Bad absolute path in config.sh" или аналог

**Где видно:** При запуске рабочего скрипта

**Причины:**
- Пути в config.sh содержат неправильные переменные
- Пути указаны абсолютно вместо относительно

**Решение:**

```bash
# Проверьте config.sh:
cat workflow/config.sh

# Все пути должны быть ОТНОСИТЕЛЬНЫМИ от ROOT репозитория:
REFERENCE="data/input/reference.fasta"  # ✓ Правильно
BAM_FILE="data/input/reads_sorted.bam"  # ✓ Правильно
OUTDIR="outputs/run_latest"             # ✓ Правильно

# Не делайте так:
REFERENCE="/home/user/blastim/genome_annotation/data/input/reference.fasta"  # ✗ Абсолютно
```

---

## Сетевые и дисковые проблемы

### ❌ "Connection timed out" при скачивании данных

**Где видно:** При запуске `01_get_data.sh`

**Причины:**
- Нет Internet соединения
- Сервер недоступен
- Сетевые фильтры блокируют доступ

**Решение:**

```bash
# Проверьте Internet:
ping google.com

# Если нет доступа, подождите или используйте VPN

# Если сервер недоступен (ftp.ncbi.nlm.nih.gov):
# Повторите позже, обычно это временное

# Запустите скрипт заново (обычно продолжит с того же места):
bash scripts/01_get_data.sh
```

---

### ❌ "No space left on device"

**Где видно:** При скачивании данных или запуске анализа

**Причины:**
- На диске нет свободного места

**Решение:**

```bash
# Проверьте свободное место:
df -h

# Если нужно освободить место:
make clean           # Удаляет результаты (outputs/)
make clean-all       # Удаляет результаты + скачанные данные

# Очистите кэш conda:
micromamba clean --all
```

---

### ❌ "Permission denied" при создании файлов

**Где видно:** При запуске скриптов анализа

**Причины:**
- Недостаточные права на папки
- Файлы принадлежат другому пользователю

**Решение:**

```bash
# Проверьте права:
ls -ld genome_annotation/
ls -ld variant_calling/

# Если нужно, измените владельца (если вы администратор):
sudo chown -R $USER:$USER genome_annotation/
sudo chown -R $USER:$USER variant_calling/
```

---

## Python и скрипты домашних заданий

### ❌ "ModuleNotFoundError: No module named 'X'" при запуске Python скрипта

**Где видно:** При запуске `90_homework_assign_*.py` или `91_homework_check.py`

**Причины:**
- Python окружение не активировано
- Python пакет не установлен

**Решение:**

```bash
# Активируйте правильное окружение:
micromamba activate ngs-annotation    # для genome_annotation
# или
micromamba activate ngs-variants      # для variant_calling

# Проверьте Python:
python3 --version
which python3

# Запустите скрипт:
python3 scripts/90_homework_assign_variants.py --help
```

---

### ❌ "Syntax Error" при запуске Python

**Где видно:** При запуске скрипта

**Причины:**
- Файл повреждён (обычно CRLF вместо LF)
- Неправильная версия Python

**Решение:**

```bash
# Конвертируйте окончания строк:
dos2unix genome_annotation/scripts/*.py
dos2unix variant_calling/scripts/*.py

# Или вручную:
sed -i 's/\r$//' genome_annotation/scripts/*.py
```

---

## Общие советы

### 📋 Когда что-то не работает, проверьте по порядку:

1. **Окружение активировано?**
   ```bash
   micromamba env list          # Проверить активное окружение
   ```

2. **Данные подготовлены?**
   ```bash
   bash scripts/01_get_data.sh
   ls -lh data/input/
   ```

3. **Конфиг скопирован?**
   ```bash
   ls -l workflow/config.sh
   ```

4. **00_check_system прошла успешно?**
   ```bash
   bash scripts/00_check_system.sh
   ```

5. **Есть свободное место?**
   ```bash
   df -h
   ```

### 💡 Полезные команды

```bash
# Проверить содержимое файла
cat workflow/config.sh

# Проверить логи последнего запуска
tail -50 outputs/run_latest/log.txt

# Увидеть какие инструменты установлены
conda list

# Тестировать конкретный инструмент
samtools --version
bcftools --version
freebayes --version
```

---

## Дополнительная помощь

Если проблема не решена:

1. Посмотрите модуль-специфичное руководство:
   - `genome_annotation/docs/troubleshooting.md`
   - `variant_calling/docs/troubleshooting.md`

2. Проверьте лог-файл:
   ```bash
   cat outputs/run_latest/log.txt
   ```

3. Откройте Issue на GitHub:
   - https://github.com/NDeeSeee/blastim_ngs/issues

4. Свяжитесь с преподавателем или TA
